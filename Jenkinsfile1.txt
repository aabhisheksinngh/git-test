pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World"'
                sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''
                archiveArtifacts artifacts: 'archiveArtifacts artifacts: \'target/*.jar\'', fingerprint: true, followSymlinks: false
            }
        }
    }
}
