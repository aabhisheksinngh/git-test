pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
         // echo 'Fail!'; exit 1
                 echo 'SUCCESS'
            }
        }
    }
 post {
  always {
      echo 'One or more steps need to be included within each condition's block.'
  }
  unstable {
      echo 'One or more steps need to be included within each condition's block.'
  }
  success {
      echo 'One or more steps need to be included within each condition's block.'
  }
  fixed {
      echo 'One or more steps need to be included within each condition's block.'
  }
}
}
