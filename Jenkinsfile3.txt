pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
         // echo 'Fail!'; exit 1
                 echo 'SUCCESS'
            }
        }
    }
   post {
  always {
      One or more steps need to be included within each condition's block.
  }
  unstable {
      One or more steps need to be included within each condition's block.
  }
  success {
      One or more steps need to be included within each condition's block.
  }
  fixed {
      One or more steps need to be included within each condition's block.
  }
}

